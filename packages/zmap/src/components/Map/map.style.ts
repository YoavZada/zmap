import type { SxProps, Theme } from "@mui/material/styles";

const container: SxProps<Theme> = {
  position: "relative",
  width: "100%",
  height: 400,
  overflow: "hidden",
};

const styles: { container: SxProps<Theme> } = { container };
export default styles;
