import type { SxProps, Theme } from "@mui/material/styles";

const loading: SxProps<Theme> = {
  display: "flex",
  justifyContent: "center",
  py: 8,
};

const styles: { loading: SxProps<Theme> } = { loading };

export default styles;
