import type { SxProps, Theme } from "@mui/material/styles";

const intro: SxProps<Theme> = { mb: 3, maxWidth: 760 };

const styles: Record<"intro", SxProps<Theme>> = { intro };

export default styles;
