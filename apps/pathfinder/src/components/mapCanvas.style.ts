import type { SxProps, Theme } from "@mui/material/styles";

const map: SxProps<Theme> = {
  width: "100%",
  height: "100%",
};

const styles: Record<"map", SxProps<Theme>> = { map };

export default styles;
